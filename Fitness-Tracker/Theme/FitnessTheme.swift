import SwiftUI

enum FitnessTheme {

    static let background = Color.black

    static let cardBackground =
        Color(white: 0.11)

    static let elevatedBackground =
        Color(white: 0.15)

    static let secondaryText =
        Color(white: 0.55)

    static let accent =
        Color(
            red: 0.36,
            green: 0.84,
            blue: 0.22
        )

    static let accentBlue =
        Color(
            red: 0.25,
            green: 0.55,
            blue: 1.0
        )
}
